interface IConfig {
    locale?: string;
}
export declare class Localize {
    private options;
    private bundle;
    private extensionPath;
    constructor(options?: IConfig);
    /**
     * translate the key
     * @param key
     * @param args
     */
    localize(key: string, ...args: string[]): string;
    init(extensionPath: string): void;
    private format;
    private resolveLanguagePack;
}
export declare function init(extensionPath: string): void;
export declare function localize(key: string, ...args: string[]): string;
export {};
